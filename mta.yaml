_schema-version: '2.0'
ID: HANAGeoSpatialTut
version: 0.0.1

modules:
 - name: geoui
   type: html5
   path: geoui

 - name: geodb
   type: hdb
   path: geodb
   requires:
    - name: hdi-container


resources:
 - name: hdi-container
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
